DATABASE_URL=postgresql://arrowmaze:arrowmaze@postgres:5432/arrowmaze
DIRECT_URL=postgresql://arrowmaze:arrowmaze@postgres:5432/arrowmaze
JWT_SECRET=docker-local-dev-secret-change-me-please
JWT_EXPIRES_IN=30d
PORT=3000
NODE_ENV=production
# ANTHROPIC_API_KEY NO va aquí: este archivo se commitea. Se inyecta desde el
# .env local (gitignored) via docker-compose.yml (ver ANTHROPIC_API_KEY ahi).
LLM_MODEL=claude-sonnet-5
LLM_TIMEOUT_MS=120000
LLM_EFFORT=low
